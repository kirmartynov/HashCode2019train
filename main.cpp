#include "util.cpp"
#include "solve.cpp"
#include "checkpiece.cpp"

int main(int argc, char** argv)  {
    
	ios::sync_with_stdio(false);
	srand(time(NULL));
    
    Input input;
    
    readInput(input, cin);

    return 0;
}